// Test stub: in vitest (node env) we run server modules directly, so the
// real `server-only` guard (which throws outside a server bundle) is aliased
// to this no-op. This does not affect the app build.
export {};
